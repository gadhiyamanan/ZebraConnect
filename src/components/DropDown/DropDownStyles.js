import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dropDown: {
    backgroundColor: "lightgray",
    padding: 20,
  },
  dropDownContainer: {
    height: 40,
    margin: 10,
  }
});
