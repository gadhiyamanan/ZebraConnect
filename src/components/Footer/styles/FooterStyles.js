import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  footerContainer: {
    // marginTop: 200,
    bottom: 0,
    alignItems: "center",
  },
  footerText: {
    textAlign: "center",
  },
  footerImage: {
    width: 200,
    height: 50,
    marginBottom: 0,
  },
});
